name = "harness-factory-dev-factory-router"
description = "Route Harness Factory changes to the smallest safe project workflow."
model = "runtime-selected"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = "Read harness/harness-spec.json and harness/team/agents/factory-router.md first.\n\nTreat the common role file as the canonical instructions. Follow its input, output, access, approval-gate, and handoff contract. Return evidence and any verification not run."
