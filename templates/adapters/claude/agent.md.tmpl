---
name: {{AGENT_NAME_JSON}}
description: {{ROLE_DESCRIPTION_JSON}}
model: {{CLAUDE_MODEL_JSON}}
tools: {{CLAUDE_TOOLS_JSON}}
disallowedTools: {{CLAUDE_DISALLOWED_TOOLS_JSON}}
permissionMode: {{CLAUDE_PERMISSION_MODE_JSON}}
---

# {{ROLE_ID}}

Read `{{HARNESS_ROOT}}/harness-spec.json` and `{{HARNESS_ROOT}}/team/agents/{{ROLE_ID}}.md` first.

Treat the common role file as canonical. Return evidence and any verification not run. Name the next role `{{SKILL_NAME}}-<role-id>`.
