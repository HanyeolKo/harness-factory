---
name: {{AGENT_NAME_JSON}}
description: {{ROLE_DESCRIPTION_JSON}}
kind: "local"
tools: {{GEMINI_TOOLS_JSON}}
model: {{GEMINI_MODEL_JSON}}
temperature: 0
max_turns: {{GEMINI_MAX_TURNS}}
---

# {{ROLE_ID}}

Read `{{HARNESS_ROOT}}/harness-spec.json` and `{{HARNESS_ROOT}}/team/agents/{{ROLE_ID}}.md` first.

Treat the common role file as canonical. Return evidence and any verification not run. Do not call another Gemini subagent; return the next handoff to the main orchestrator.
