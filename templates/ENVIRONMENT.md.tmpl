# ENVIRONMENT — {{TARGET}}

> 모든 커맨드는 복사해서 그대로 실행 가능해야 한다. 설명문으로 적지 않는다.

## 실행 커맨드

| 목적 | 커맨드 | 비고 |
|---|---|---|
| 빌드 | `{{BUILD_COMMAND}}` | |
| 실행 | `{{RUN_COMMAND}}` | |

## 검증 커맨드 (evaluator — EVAL-LOOP에서 참조)

| 목적 | 커맨드 | pass 조건 |
|---|---|---|
| 주 검증 | `{{EVALUATOR_COMMANDS}}` | exit 0 |
| 정적 검사 | `{{LINT_COMMAND}}` | exit 0 |

## 디렉토리 맵 (대상 프로젝트)

```
{{PROJECT_TREE}}
```

## 기존 오프로딩 자산 (재발명 금지 — 원칙 3)

| 자산 | 위치 | 용도 |
|---|---|---|
| {{EXISTING_SCRIPTS}} | | |

## 금지사항

- {{FORBIDDEN}}
<!-- 예: "main 브랜치 직접 push 금지", "prod 환경변수 파일 수정 금지" -->
- 게이트 대상 단계({{GATES}})는 인간 승인 없이 수행 금지
