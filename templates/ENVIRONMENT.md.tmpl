# ENVIRONMENT — {{TARGET}}

> Every command must be ready to copy and run. Do not replace a command with prose.

## Run commands

| Purpose | Command | Notes |
|---|---|---|
| Build | `{{BUILD_COMMAND}}` | |
| Run | `{{RUN_COMMAND}}` | |

## Verification commands

| Purpose | Command | Pass condition |
|---|---|---|
| Primary evaluator | `{{EVALUATOR_COMMANDS}}` | exit 0 |
| Static checks | `{{LINT_COMMAND}}` | exit 0 |
| Self-evaluation trigger | `python {{HARNESS_ROOT}}/triggers/check_self_evaluation.py {{HARNESS_ROOT}}` | exit 0; compact `none|targeted|full` JSON |

## Target project map

```
{{PROJECT_TREE}}
```

## Existing offloading assets

| Asset | Location | Use |
|---|---|---|
| {{EXISTING_SCRIPTS}} | | |

## Restrictions

- {{FORBIDDEN}}
<!-- Example: "Do not push directly to main" or "Do not edit production environment files." -->
- Never perform gated steps ({{GATES}}) without human approval.
