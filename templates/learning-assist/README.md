# Learning Assist templates

Learning Assist is user-requested and non-blocking. Generate session files only when the user asks for explanation, study, or a comprehension check.

Use `templates/reports/report-style.json.tmpl` for the same plain-language-first wording used by normal Change Reports.
