# Context Budget

Use completed work units as the observable budget. A unit should use at most one quarter of a session. At 80%, checkpoint and finish only the current unit. At 100%, persist `next_action` and stop or replace the session. Read indexes first and split before instruction or memory limits.
