# Harness Effect Evaluation Loop

Run only after valid checker routing. `targeted` uses the exact deterministic mapping in `evaluation/suites/targeted.json`. `full` compares baseline, control, and treatment with the same evaluator and conditions. Freeze `trigger.json` before evaluation and ACK every completed run.
