# Improvement Loop

Open improvement only after full attribution or an explicit evidence-backed request. Change one hypothesis and at most two components, update canonical meaning first, reproject all selected providers, rerun verification and the same full suite, and accept only `improved` or pre-approved `neutral`.
