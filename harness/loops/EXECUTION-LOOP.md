# Execution Loop

Define the evaluator before work. Route, implement, run raw evidence, record the verdict, update state once, write the Change Report, then run the deterministic checker. Retry and gate stops follow recovery policy.
